{
  "name": "newsbot-chatgpt",
  "version": "1.1.0",
  "private": true,
  "workspaces": [
    "lib-news",
    "app-aws"
  ],
  "scripts": {
    "build": "npm run build --workspace lib-news",
    "test": "npm run test --workspace lib-news",
    "aws:deploy": "npm run deploy --workspace app-aws",
    "aws:diff": "npm run diff --workspace app-aws"
  }
}
